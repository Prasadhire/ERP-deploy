apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: erp-ingress
  namespace: erp-ns
spec:
  rules:
  - http:
      paths:
      - path: /ERP
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80

